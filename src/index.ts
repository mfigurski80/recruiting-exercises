import Inventory from './interfaces/Inventory'
import Warehouses from './interfaces/Warehouses'

var data: Warehouses = {
    'tx': { 'apples': 2, 'shovels': 5 },
    'fl': { 'apples': 1 }
}

console.log(data)
